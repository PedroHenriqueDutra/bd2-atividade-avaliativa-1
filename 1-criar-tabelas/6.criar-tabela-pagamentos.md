### Crie a tabela pagamentos com:

pagamento_id como INT AUTO_INCREMENT e chave primária

venda_id como INT não nulo

tipo_pagamento como VARCHAR(50) não nulo

valor como DECIMAL(12,2) não nulo

data_pagamento como DATE não nulo
