### Crie a tabela clientes com:

`cliente_id` como `INT AUTO_INCREMENT` e chave primária

`nome` como `VARCHAR(100)` não nulo

`email` como `VARCHAR(150)` não nulo e único

`telefone` como `VARCHAR(20)`
