### Crie a tabela vendas com:

venda_id como INT AUTO_INCREMENT e chave primária

cliente_id como INT não nulo

data_venda como DATE não nulo com DEFAULT CURRENT_DATE

valor_total como DECIMAL(12,2) não nulo com DEFAULT 0.00

