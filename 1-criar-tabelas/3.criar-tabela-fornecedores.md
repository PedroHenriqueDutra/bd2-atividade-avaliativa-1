### Crie a tabela fornecedores com:

fornecedor_id como INT AUTO_INCREMENT e chave primária

nome como VARCHAR(120) não nulo

cnpj como CHAR(14) não nulo e único

email como VARCHAR(150)

telefone como VARCHAR(20)
