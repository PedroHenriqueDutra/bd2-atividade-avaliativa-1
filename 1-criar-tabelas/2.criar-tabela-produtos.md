### Crie a tabela produtos com:

produto_id como INT AUTO_INCREMENT e chave primária

nome como VARCHAR(120) não nulo

preco como DECIMAL(10,2) não nulo

estoque como INT não nulo com DEFAULT 0

ativo como BOOLEAN com DEFAULT TRUE

