A tabela alunos possui a coluna ativo definida como VARCHAR(5), o que não é adequado para representar valores booleanos.

Altere a estrutura da tabela para que a coluna ativo passe a ser do tipo BOOLEAN.