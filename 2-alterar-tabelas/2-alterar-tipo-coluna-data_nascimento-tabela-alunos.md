A tabela alunos possui a coluna data_nascimento definida como VARCHAR(20), o que não é adequado para representar datas.

Altere a estrutura da tabela para que a coluna data_nascimento passe a ser do tipo DATE.